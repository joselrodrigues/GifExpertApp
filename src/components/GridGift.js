import React from 'react';
import PropTypes from 'prop-types';

const GridGift = props => {
    return (
        <div>

        </div>
    );
};

GridGift.propTypes = {
    
};

export default GridGift;